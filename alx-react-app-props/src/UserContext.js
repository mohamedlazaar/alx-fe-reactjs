// src/UserContext.js
import React from 'react';

// Create a new Context for user data
const UserContext = React.createContext();

// Export the UserContext to be used in other components
export default UserContext;
